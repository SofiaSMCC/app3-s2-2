 version: "3.3"

 services:
   api-students:
     build: ./api-students
     image: sofiasm19/api-students
     ports:
       - 8000:8000
   websimple:
     build: ./websimple
     image: sofiasm19/websimple
     ports:
       - 8080:80
